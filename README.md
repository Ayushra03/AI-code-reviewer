# AI-code-reviewer
## I have created a streamlit app that will point all error in your code and also show the corrected code ##
To run in VS code intall all dependencies and then type "streamlit run app.py" the app will start running on localhost:8501

